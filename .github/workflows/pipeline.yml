name: CI/CD Pipeline

on:
    push:
        branches: [ master ]
    workflow_dispatch:

jobs:
    terraform:
        uses: ./.github/workflows/terraform.yml
        secrets: inherit

    integration:
        uses: ./.github/workflows/integration.yml
        secrets: inherit
        needs: terraform
